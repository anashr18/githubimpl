// src/server.rs

pub struct HttpServer {
    // eventually this will hold TcpListener, config, etc.
}

impl HttpServer {
    pub fn new() -> Self {
        HttpServer { }
    }

    pub fn run(&self) {
        // placeholder run method
        println!("Server would start here...");
    }
}
